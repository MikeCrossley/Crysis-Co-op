EventSynchronizer = {
	Editor={
		-- RaZoR: Just because having an icon is good.
		Icon="TagPoint.bmp",
	},
}

-- Don't know if this shit is even required, doubt it.
function EventSynchronizer:OnInit()
end
